# Band SDK with all four framework adapters
band-sdk[anthropic,langgraph,crewai,pydantic-ai]>=0.5.0

# Google ADK adapter (for Gemini-backed agents)
google-adk>=0.3.0

# Core
python-dotenv>=1.0.0
pyyaml>=6.0
httpx>=0.27.0

# LLM provider SDKs (used via OpenAI-compatible endpoints)
langchain-openai>=0.2.0
openai>=1.50.0
google-genai>=0.5.0

# Frontend
streamlit>=1.40.0
pandas>=2.2.0
plotly>=5.24.0

# Document parsing (uploaded scripts, budgets, contracts)
pypdf>=4.0.0
python-multipart>=0.0.9
